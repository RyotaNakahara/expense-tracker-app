# 依存関係 (Dependencies)
node_modules/
/.pnp
.pnp.js

# テスト (Testing)
/coverage

# プロダクションビルド (Production)
/build
/dist

# Vite
.vite/
*vite.config.js.timestamp-*
*vite.config.ts.timestamp-*

# その他 (Misc)
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# 環境変数ファイル (Environment variables)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.local

# ログファイル (Logs)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
firebase-debug.log
firestore-debug.log
ui-debug.log

# エディタディレクトリとファイル (Editor directories and files)
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/
*.sublime-project
*.sublime-workspace

# Firebase
.firebase/
firebase-export-*/
.firebaserc.backup
firebase-*.cache

# npmキャッシュディレクトリ（オプション） (Optional npm cache directory)
.npm

# ESLintキャッシュ（オプション） (Optional eslint cache)
.eslintcache

# REPL履歴（オプション） (Optional REPL history)
.node_repl_history

# 'npm pack'の出力 (Output of 'npm pack')
*.tgz

# Yarn Integrity ファイル (Yarn Integrity file)
.yarn-integrity

# parcel-bundlerキャッシュ (parcel-bundler cache) (https://parceljs.org/)
.cache
.parcel-cache

# Next.js (使用する場合) (if using Next.js)
.next/
out/

# Nuxt.js (使用する場合) (if using Nuxt.js)
.nuxt

# Gatsby (使用する場合) (if using Gatsby)
.cache/
# Note: public/ ディレクトリはReact + Viteアプリではコミット対象です

# Storybook (使用する場合) (if using Storybook)
storybook-static

# TypeScript
*.tsbuildinfo

# デプロイ関連 (Deployment)
.vercel
.netlify
.railway

# 一時ファイル (Temporary files)
*.tmp
*.temp
*.log

# ローカル設定ファイル (Local configuration files)
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
